# Entity Schema

`entity.schema.json` documents the common YAML front-matter contract using JSON Schema. The standard-library validator implements its V1 checks without requiring a JSON Schema or YAML package; entity-specific narrative fields remain governed by templates and ontology.
